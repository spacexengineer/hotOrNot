// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

export default {
  pageExtensions: ["tsx", "ts", "jsx", "js"],
  // If you're using a `src` folder, you might need to set the base path like this:
  // However, this is not usually required unless you're using a non-standard setup.
  // basePath: '/src',
};
